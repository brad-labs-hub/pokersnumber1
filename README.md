# Audiobook Player (Vercel)

A minimal, native-like audiobook player built with Next.js (App Router).

## Dev

```bash
npm install
npm run dev
```

## Deploy

- Import this repo into Vercel and deploy.
- The audio + album art are currently configured in `lib/media.ts`.

